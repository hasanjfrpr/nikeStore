<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.digikala">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Digikala"
        android:usesCleartextTraffic="true">
        <activity android:name=".feature.main.profile.favoriteproductList.FavoriteListActivity"></activity>
        <activity android:name=".feature.main.shiping.ShippingActivity" />
        <activity android:name=".feature.main.checkout.CheckOutActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="expertdevelopers.ir"
                    android:path="/appCheckout"
                    android:scheme="nike" />
            </intent-filter>
        </activity>
        <activity
            android:name=".feature.main.auth.AuthActivity"
            android:theme="@style/Theme.nike.backgroundLogin" />
        <activity android:name="com.e.ProductListActivity" />
        <activity android:name=".feature.main.profile.comment.AllCommentListActivity" />
        <activity
            android:name=".feature.main.product.ProductDetailActivity"
            android:parentActivityName=".feature.main.main.MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".feature.main.main.MainActivity" />
        </activity>
        <activity android:name=".feature.main.main.MainActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>